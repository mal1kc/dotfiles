local monitor_cfg = require("monitors")
local autostart_cfg = require("autostart")

local binds_cfg = require("binds")
local envs_vars_cfg = require("vars")
local window_rules_cfg = require("window_rules")
local anims_cfg = require("anims")

local plugins_cfg = require("plugins")
