bind = SUPER, comma, hyprexpo:expo, toggle # can be: toggle, off/disable or on/enable
# bind = $mainMod, mouse_down, hyprscrolling:move, -200
# bind = $mainMod, mouse_up,   hyprscrolling:move, +200
# bind = $mainMod, mouse_down,  layoutmsg, move -200
# bind = $mainMod, mouse_up,   layoutmsg, move +200
# bind = $mainMod, right,   layoutmsg, focus r
# bind = $mainMod, left,  layoutmsg, focus l
# bind = $mainMod z, period, layoutmsg, promote
# bind = $mainMod SHIFT, right, layoutmsg, movewindowto r
# bind = $mainMod SHIFT, left, layoutmsg, movewindowto l
# bind = $mainMod SHIFT, up, layoutmsg, movewindowto u
# bind = $mainMod SHIFT, down, layoutmsg, movewindowto d
plugin {
  hyprwinwrap {
      # class is an EXACT match and NOT a regex!
      class = hyprwinwrap
  }

  # Desktop
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1
    }
  # hyprbars {
  #   # example config
  #   bar_height = 20
  #
  #   # example buttons (R -> L)
  #   # hyprbars-button = color, size, on-click
  #   hyprbars-button = rgb(ff4040), 10, 󰖭, hyprctl dispatch killactive
  #   hyprbars-button = rgb(eeee11), 10, , hyprctl dispatch fullscreen 1
  # }
  
  # hyprscrolling {
  # fit = all
  #  }
}

